[tool.poetry]
name = "retroarcherdiscord-bot"
version = "0.1.0"
description = "Bot for RetroArcher discord server"
authors = ["ReenigneArcher"]

[tool.poetry.dependencies]
python = "^3.8"
py-cord = "2.0.0"
Flask = "2.1.3"
igdb-api-v4 = "0.0.5"
python-dotenv = "0.20.0"
requests = "2.28.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
